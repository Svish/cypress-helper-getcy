{
  "name": "cypress-helper-getcy",
  "version": "0.0.1",
  "description": "Simple Cypress command for getting elements via `data-cy` attributes",
  "keywords": [
    "cypress",
    "helper"
  ],
  "license": "MIT",
  "scripts": {
    "prepare": "tsc",
    "start": "run-s -s prepare cypress:open",
    "test": "run-s -s prepare lint:* cypress:run",
    "cypress:open": "cypress open --project test",
    "cypress:run": "cypress run --project test",
    "lint:ts": "tsc --noEmit",
    "lint:prettier": "prettier --ignore-path .gitignore --check \"**/*\""
  },
  "author": "Torleif Berger <torleif@outlook.com> (https://www.geekality.net)",
  "repository": {
    "type": "git",
    "url": "https://github.com/svish/cypress-helper-getcy"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "cypress": ">=0.20.0"
  },
  "devDependencies": {
    "cypress": "^3.3.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.18.2",
    "typescript": "^3.5.2"
  }
}
